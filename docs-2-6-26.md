# Updates 2-26-2026

Built static Roq site using quarkus-roq extension:
`quarkus create app roq-test -x=io.quarkiverse.roq:quarkus-roq`

MkDocs file structure is much different than Roq's, let's try to build an MkDocs site with Roq.

Originally, trying to run `quarkus dev` on smallrye-stork gives us an error,
we have to install maven and the Roq extension.

`sudo apt install maven`
`quarkus ext add quarkus-roq`

Running `quarkus dev` still gives us an error:
```
[[1;31mERROR[m] No plugin found for prefix 'quarkus' in the current project and in the plugin groups [org.apache.maven.plugins, org.codehaus.mojo] available from the repositories [local ($HOME/.m2/repository), central (https://repo.maven.apache.org/maven2)] -> [1m[Help 1][m
```
It appears as though we can't locate the quarkus-roq dependency specified in our pom:
```xml
    <dependency>
        <groupId>io.quarkiverse.roq</groupId>
        <artifactId>quarkus-roq</artifactId>
        <version>2.0.4</version>
    </dependency>
```
```
After running `mvn install` and `mvn dependency:resolve`, we still get an issue:
[[1;31mERROR[m] Failed to execute goal [32morg.revapi:revapi-maven-plugin:0.15.1:check[m [1m(check-compatibility)[m on project [36mstork-parent[m: [1;31mThe plugin org.revapi:revapi-maven-plugin:0.15.1 requires Maven version 3.8.1[m -> [1m[Help 1][m
```

