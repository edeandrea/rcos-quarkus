```sh
./mvnw -f docs/ <mavenPrefix>:<goalPrefix>
```

I wasn't sure which prefixes to use to run the Roq goals. It looks like in `pom.xml`, Roq is listed as a **dependency** rather than a **plugin**, whereas many if not all of the other integrated libraries for smallrye-stork are listed as both, I'm just unsure as to why-I will seek clarification on this point. 
Revisiting the Roq test site, I tried 
```./mvnw -f docs/ io.quarkiverse.roq:quarkus-roq```
but it is still giving me issues regarding the pom.xml file. For some time I considered whether I was using the right command but realized due to the completely different structure of MkDocs vs. Roq, it is pointless to try and compile it in its current form.

Will confirm if 
`./mvnw -f docs/ io.quarkiverse.roq:quarkus-roq`
is the correct command.


Ultimately, I'm shifting to manual migration before building with Roq solely. 
Since @RyanJK5 is working on migrating the smallrye-stork document, I will focus on the lang4j workshop, although we will work together for common strategies.

Update: @RyanJK5 has been using
`./mvn -f docs/ quarkus:dev` which I assume is the same build goal as `quarkus dev`. I will return to this once more progress on migration has been made.

> Note: I will move my personal migration steps into a separate document.
