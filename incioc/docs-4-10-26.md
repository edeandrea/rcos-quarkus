# Late March to Early April

- The theme must match
  - Diagrams are not rendered correctly
  - Code blocks are not rendered correctly

UPDATE: @RyanJK5 is taking care of this!

Tested migration script so far with clean installation of `content` dir; script to escape "{}" characters seems functional, but some links break:

> Multi-agent collaboration (workflow{{target="_blank"}} or supervisor-based{{target="_blank"}}
- We must delete {target="_blank"}
  - Notably we cannot ignore this, as the single braces causes our page to error

UPDATE: Script has been updated to reflect this change!

In MkDocs, images are kept in `docs/images`, in Roq, they are kept in `public/images`, (with `/docs` as the root directory for both). I will update the script to reflect this.
- mp4 files are also present in this directory, for MkDocs.

I found this line to be problematic:
> - Designed to be composed with for **workflows** or be invoked by a supervisor — agents can be composed together (more on this in [Step 02](step-02.md))
Here, in `section-2/step-01.md`, we are supposed to redirect to `section-2/step-02.md`, but instead we go to `section-2/step-01/step-02.md`, which can't be found...looking into this

