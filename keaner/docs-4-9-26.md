# 4/9/26

## Kroki Bug

I discovered a bug where diagrams were not rendering properly. I fixed this by adding an applcation.properties file and explicitly setting `KROKI_URL`.

## Deobfuscation

Much of the MkDocs build output is obfuscated because it was min-ified. I ran prettier on the files to make them slightly more readable, though they still use short variable and function names.