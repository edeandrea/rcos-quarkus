# 3/10/26

## Diagrams

I began by changing all diagrams throughout the website to use the #diagram tag instead of including the generated image directly. Once the theme is more accurate, I want to further investigate the sizing of these diagrams, since they appear much larger on the Roq site than on MkDocs.

## Theme

The Stork MkDocs page uses the built-in material theme for displaying its markdown content. Currently, Roq only has the default and resume themes offered as plugins, so I will work on creating a reusable material theme that can be included as an extension in the same way the default theme is.

### Project Setup
I began by creating a new `quarkus-roq-theme-material` directory for storing the material theme and mimiced the implementation of the default theme when designing the pom.xml file and general project structure.

### Integrating into Roq

The next step was to replace the default theme with my custom material theme. I changed which theme was included in the pom.xml file, which immediately produced an error since the web page was looking for a theme with no components defined.

I began implementing the basic page files so the site could be rendered in some capacity. I created small html files for representing the headers, sidebar, main page, etc. The site was functional after this, though heavily lacking in terms of styling and readability due to the lack of css implementation.

### Porting MkDocs Theme

I copied much of the csv content from MkDocs' material theme and emulated the website fairly closely. There are some areas remaining where the theme does not perfectly match that I have to investigate:

- Sidebar
- Header
- Search bar
- Misc. spacing

Additionally, there is logic that will have to be implemented in JavaScript (such as the light/dark mode button) that I have not yet worked on.